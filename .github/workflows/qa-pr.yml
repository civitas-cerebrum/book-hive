name: QA

on:
  workflow_run:
    workflows: ["Test"]
    types: [completed]
  issue_comment:
    types: [created]

jobs:
  pr-analysis:
    name: PR Analysis
    runs-on: self-hosted
    if: >
      github.event_name == 'workflow_run' ||
      (github.event_name == 'issue_comment' &&
       github.event.issue.pull_request &&
       contains(github.event.comment.body, '@qa'))
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}

      - name: Set check status (pending)
        if: github.event_name == 'workflow_run'
        run: |
          curl -s -X POST \
            -H "Authorization: token $GH_PAT" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${{ github.repository }}/statuses/${{ github.event.workflow_run.head_sha }}" \
            -d '{"state":"pending","context":"QA / PR Analysis","description":"Analyzing...","target_url":"https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"}'
        env:
          GH_PAT: ${{ secrets.GH_PAT }}

      - name: Start app
        run: |
          docker compose down 2>/dev/null || true
          docker compose up -d --build --wait
        timeout-minutes: 5

      - name: Wait for app
        run: |
          for i in $(seq 1 30); do
            curl -s http://localhost:8080/api/health | grep -q healthy && exit 0
            sleep 2
          done

      - name: Run QA Agent
        run: |
          docker run --rm --network host \
            -e ANTHROPIC_API_KEY="${ANTHROPIC_API_KEY}" \
            -e GITHUB_TOKEN="${GH_PAT}" \
            -e GITHUB_EVENT_NAME="${GITHUB_EVENT_NAME}" \
            -e GITHUB_EVENT_PATH="/event.json" \
            -e GITHUB_REPOSITORY="${GITHUB_REPOSITORY}" \
            -v "${GITHUB_EVENT_PATH}:/event.json:ro" \
            -v "$(pwd):/workspace:ro" \
            magnum-opus:latest pr .magnum-opus/config.yml
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GH_PAT: ${{ secrets.GH_PAT }}

      - name: Set check status (success)
        if: github.event_name == 'workflow_run' && success()
        run: |
          curl -s -X POST \
            -H "Authorization: token $GH_PAT" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${{ github.repository }}/statuses/${{ github.event.workflow_run.head_sha }}" \
            -d '{"state":"success","context":"QA / PR Analysis","description":"Analysis complete","target_url":"https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"}'
        env:
          GH_PAT: ${{ secrets.GH_PAT }}

      - name: Set check status (failure)
        if: github.event_name == 'workflow_run' && failure()
        run: |
          curl -s -X POST \
            -H "Authorization: token $GH_PAT" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${{ github.repository }}/statuses/${{ github.event.workflow_run.head_sha }}" \
            -d '{"state":"failure","context":"QA / PR Analysis","description":"Analysis failed","target_url":"https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"}'
        env:
          GH_PAT: ${{ secrets.GH_PAT }}

      - name: Stop app
        if: always()
        run: docker compose down 2>/dev/null || true
